package application;

public class Connection {
    Node destination;
    double distance;

    public Connection(Node destination, double distance) {
        this.destination = destination;
        this.distance = distance;
    }
}